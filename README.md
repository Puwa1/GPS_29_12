# GPS Tracking — Modular Version

แอป GUI สำหรับตรวจสอบเส้นทางและสถานะการขับเคลื่อนจากไฟล์ CSV โดยใช้ `CustomTkinter` และ `TkinterMapView` สามารถเลือกช่วงเวลา กำหนดจำนวนจุดที่จะวาด เปลี่ยนสไตล์แผนที่ ซูมไปที่ตำแหน่งรถ และดูบันทึกสถานะได้แบบเรียลไทม์

## ฟีเจอร์หลัก
- เปิดไฟล์ `CSV` และจัดการช่วงเวลาได้ (`All Days` หรือเลือกวันที่เฉพาะ)
- จำกัดจำนวนจุดที่วาดบนแผนที่เพื่อให้ทำงานลื่นขึ้น
- เลือกช่วงแถว (`Start`–`End`) เพื่อดูบางส่วนของข้อมูล
- แสดงเส้นทาง, จุดเริ่ม (`Start`) และจุดสิ้นสุด (`End`)
- แสดงตำแหน่งรถพร้อมไอคอน และสถานะ `Running / Idling / Engine Off`
- สไลเดอร์ `Timeline` สำหรับเลื่อนดูข้อมูลตามเวลา
- ปรับสเกลระยะทางบนแผนที่แบบอัตโนมัติ
- เปลี่ยนสไตล์แผนที่: `OpenStreetMap`, `Google Normal`, `Google Satellite`
- ปุ่มซูมไปที่รถ, ล้างหน้าจอ, และคัดลอกพิกัดไปคลิปบอร์ด

## โครงสร้างโปรเจค
- `main.py` จุดเริ่มต้นและ UI หลักของแอป
- `utils.py` โหลดไอคอนรถ และประมวลผลไฟล์ CSV
- `components.py` วิดเจ็ตสเกลบาร์แบบโปร่งใส
- `config.py` ตั้งค่าธีม สี และค่าทั่วไปของระบบ
- `main_test.py` เวอร์ชันทดสอบ/ต้นแบบ UI (ไม่จำเป็นต้องใช้ในการรันหลัก)

## ข้อกำหนดระบบ
- แนะนำ `Python 3.10+`

## การติดตั้งแพ็กเกจ
ติดตั้งไลบรารีที่จำเป็นด้วยคำสั่ง:

```bash
pip install customtkinter tkintermapview pandas pillow
```

หมายเหตุ:
- หากสภาพแวดล้อมบางเครื่องต้องการ `darkdetect` (ใช้ร่วมกับ `customtkinter`) ให้ติดตั้งเพิ่มด้วย `pip install darkdetect`

## วิธีใช้งาน
1. รันแอปด้วยคำสั่ง:
   ```bash
   python main.py
   ```
2. คลิกปุ่ม “เลือกไฟล์ CSV”
3. เลือกช่วงวันที่ หรือใช้ตัวเลือก `All Days`
4. ตั้งค่าช่วงแถว (`Start`–`End`) และ/หรือจำนวนจุดสูงสุดที่จะวาด
5. กด “Apply Settings” เพื่อประมวลผลและวาดข้อมูลบนแผนที่
6. ใช้สไลเดอร์ `Timeline` เพื่อเลื่อนดูตำแหน่งตามเวลา
7. เปลี่ยนสไตล์แผนที่ได้จากเมนูบนมุมแผนที่
8. กด “ซูมไปที่รถ”, “ล้างหน้าจอ”, หรือ “📋” เพื่อคัดลอกพิกัดเมื่อจำเป็น

## รูปแบบไฟล์ CSV ที่รองรับ
ต้องมีคอลัมน์อย่างน้อย:
- พิกัดละติจูด: `lat`
- พิกัดลองจิจูด: `long` หรือ `lon` หรือ `lng`
- เวลา: `r-time` หรือ `time` (รองรับ day-first และจะถูกแปลงเป็น `datetime`)

คอลัมน์เสริม (ถ้ามี):
- ความเร็ว: `gps_speed` หรือ `speed`
- สถานะเครื่องยนต์: `acc-on` หรือ `acc` (ค่าที่มี “1/ON” หมายถึงเครื่องยนต์ติด)

การเข้ารหัสไฟล์:
- พยายามอ่านเป็น `utf-8` หากล้มเหลวจะลองอ่านแบบ `cp874`

เงื่อนไขการแสดงผล:
- จุดที่ `lat == 0.0` หรือ `lon == 0.0` จะไม่ถูกวาด
- หากข้อมูลว่าง หรือขาดคอลัมน์ที่จำเป็น แอปจะรายงานข้อผิดพลาด

## คีย์ลัด
- `Left` เลื่อนย้อนเวลา
- `Right` เลื่อนไปข้างหน้า

## ทิปเพิ่มเติม
- หากมีไฟล์ `car.png` ในโฟลเดอร์เดียวกับโปรเจค แอปจะใช้เป็นไอคอนรถอัตโนมัติ หากไม่มี ระบบจะสร้างไอคอนเวคเตอร์สำรองให้
- การกำหนด “จำนวนจุดสูงสุด” ช่วยลดภาระการวาดบนแผนที่และทำให้ UI ตอบสนองดีขึ้นเมื่อข้อมูลมีจำนวนมาก

## ปัญหาที่พบบ่อย
- ไม่พบคอลัมน์ที่จำเป็น: ตรวจสอบชื่อคอลัมน์ให้ตรงตามที่ระบุ
- อ่านไฟล์ไม่ได้: ตรวจสอบรูปแบบการเข้ารหัส (`utf-8`/`cp874`) หรือความถูกต้องของข้อมูลเวลา
- แผนที่ Google: การใช้งาน tile server ของ Google ผ่าน `TkinterMapView` อาจมีการเปลี่ยนแปลงได้ตามเงื่อนไขของผู้ให้บริการ

